/**
 * CRT boot markup/CSS lives in index.html + crt-power-on.css.
 * The monitor texture uses canvas power-on in MySpaceScreen (html-to-image taints WebGL canvases).
 * Toggle `is-playing` on #crt-power-on-root stays in sync for future wiring.
 */
export const CRT_POWER_ON_MS = 1150;
